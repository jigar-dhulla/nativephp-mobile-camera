{
    "name": "nativephp/camera",
    "description": "Camera plugin for NativePHP Mobile (Photo capture, Video recording, Gallery picker)",
    "type": "nativephp-plugin",
    "license": "MIT",
    "authors": [
        {
            "name": "NativePHP",
            "email": "info@nativephp.com"
        }
    ],
    "require": {
        "php": "^8.2",
        "nativephp/mobile": "*"
    },
    "require-dev": {
        "pestphp/pest": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "NativePHP\\Camera\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "NativePHP\\Camera\\CameraServiceProvider"
            ]
        },
        "nativephp": {
            "manifest": "nativephp.json"
        }
    },
    "scripts": {
        "test": "pest"
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    }
}